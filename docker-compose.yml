#version: "3"

services:
  wppconnect:
    build:
      context: .
    volumes:
      - ./config.ts:/usr/src/wpp-server/config.ts
      - ./wppconnect_tokens:/usr/src/wpp-server/tokens
    ports:
      - "${port}:${port}"
    restart: always

volumes:
  wppconnect_tokens: {}      
